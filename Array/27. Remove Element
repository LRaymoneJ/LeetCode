public int removeElement(int[] nums, int val) {
     int result = 0;
        int len = nums.length;
        for (int i = 0; i < len; i++) {
            if (nums[i] != val){
                nums[result] = nums[i];
                result ++;
            }
        }
        return result;
    }
