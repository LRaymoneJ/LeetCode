public boolean isPowerOfFour(int num) {
        boolean flag = true;
        if (num <= 0)
            return false;
        while (num > 1) {
            if (num % 4 != 0) {
                flag = false;
                break;
            }
            num /= 4;
        }
        return flag;
    }
